# Holdout velvet headroom

The unopened holdout query token is `velvetcomet`. Its independent relation
cluster mirrors only the registered headroom stratum, with disjoint nodes, URLs,
and credited edge identity.

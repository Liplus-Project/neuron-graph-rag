# Holdout harbor ceiling

The unopened holdout query token is `harborlattice`. Its independent relation
cluster mirrors only the registered ceiling stratum, with disjoint nodes, URLs,
and credited edge identity.
